
import { defineDataSet } from 'ai-dataset';

const layoutMap = {
  '居左': 'flex-start',
  '居中': 'center',
  '居右': 'flex-end'
}
export default defineDataSet((utils)  => {
  const result = {}
  /** TODO:增加按钮、修改标题内容 */
  result['添加'] = []

  return result
})
